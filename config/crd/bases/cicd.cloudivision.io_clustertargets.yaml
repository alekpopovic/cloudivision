---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: clustertargets.cicd.cloudivision.io
spec:
  group: cicd.cloudivision.io
  names:
    kind: ClusterTarget
    listKind: ClusterTargetList
    plural: clustertargets
    singular: clustertarget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.reachable
      name: Reachable
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterTarget describes an experimental external cluster connection. It is health-checked only.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              context:
                type: string
              defaultNamespace:
                type: string
              displayName:
                minLength: 1
                type: string
              kubeconfigSecretRef:
                properties:
                  key:
                    type: string
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              labels:
                additionalProperties:
                  type: string
                type: object
              type:
                enum:
                - build
                - deploy
                - both
                type: string
            required:
            - displayName
            - type
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Ready
                - Unreachable
                type: string
              reachable:
                type: boolean
              version:
                type: string
            required:
            - reachable
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

