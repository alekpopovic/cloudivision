apiVersion: cicd.cloudivision.io/v1alpha1
kind: BuildRun
metadata:
  name: conformance-failure
  namespace: __NAMESPACE__
spec:
  projectRef: conformance
  repositoryRef: conformance
  pipelineTemplateRef: conformance-failure
  revision: __REVISION__
  branch: main
  triggeredBy:
    type: manual
    actor: conformance
  image:
    repository: example.invalid/conformance
    tag: failure
  executor: job
  gitOps:
    enabled: false
